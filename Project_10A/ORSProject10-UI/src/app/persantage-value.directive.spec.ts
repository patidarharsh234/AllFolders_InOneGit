import { PersantageValueDirective } from './persantage-value.directive';

describe('PersantageValueDirective', () => {
  it('should create an instance', () => {
    const directive = new PersantageValueDirective();
    expect(directive).toBeTruthy();
  });
});
